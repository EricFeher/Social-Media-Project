import { SchoolUserConnector } from './school-user-connector.model';

describe('SchoolUserConnector', () => {
  it('should create an instance', () => {
    expect(new SchoolUserConnector()).toBeTruthy();
  });
});